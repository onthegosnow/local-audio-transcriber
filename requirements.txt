# Local Audio Transcriber — Python dependencies
#
# faster-whisper pulls in ctranslate2 + av (PyAV), which bundle their own
# audio decoding, so a system ffmpeg binary is NOT required.
faster-whisper>=1.0.0

# Desktop GUI toolkit (Qt for Python, pip-installable, LGPL).
PySide6>=6.6.0

# Used to talk to the local Ollama HTTP API.
requests>=2.31.0
